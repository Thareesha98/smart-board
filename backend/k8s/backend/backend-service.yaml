apiVersion: v1
kind: Service
metadata:
  name: sbms-backend
  namespace: sbms
spec:
  type: NodePort     # ← YOU MISSED THIS LINE
  selector:
    app: sbms-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30086   # valid only when type = NodePort

